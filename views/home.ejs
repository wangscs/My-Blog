<%- include("partials/header") -%>

<div>
  
  <!-- <% for(var i = 0; i<posts.length; i++){ %>
    <div>
      <% let stitle = posts[i].title; %>
      <% let scontent = posts[i].content; %>
      <h4><%= stitle %></h4>
      <p><%= scontent %></p>
    </div>
  <% } %> -->

  <% posts.forEach(function(post){ %>
    <h1><%= post.title %></h1>
    <p><%= post.content %></p>
  <% }) %>
</div>



<%- include("partials/footer") -%>